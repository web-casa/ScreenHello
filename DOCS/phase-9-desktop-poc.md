# Experimental desktop entry

The desktop implementation lives in `desktop/`, `src/desktop/` and `src-tauri/`. This branch is for browser compatibility testing only; its workflow does not certify desktop capture, native file operations, installers or release readiness.
