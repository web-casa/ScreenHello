# Desktop framework

The experimental desktop entry uses Tauri 2 with the shared browser editor and scoped native commands. This browser-test branch does not provide a signed desktop release. Native permissions, installers, signing and store submission require separate verification.
